<?php
/**
 * 算术操作符：+，-，*，/，%（取余）
 * 字符串操作符：. 用于字符串连接
 * 赋值操作符：=，+=，-=，*=，/=，.=，%=
 * 逻辑操作符：&&或and（逻辑与）同真为真   ||或or（逻辑或）一真则真   xor（逻辑异或） 一真一假为真   !(逻辑非)  
 * 比较操作符：'<','>',<=,>=,==,!=,===(恒等)，!== 
 * 条件操作符（三元操作符）：?: 用于根据一个表达式在另两个表达式中选择一个
 */

    //前置递增
    $a = 3;
    $b = ++$a;
    echo $a."\n";
    echo $b."\n";
    //后置递增
    $c = 3;
    $d = $c++;
    echo $c."\n";
    echo $d."\n";

    //条件操作符
    $value = 100;
    echo ($value == true)?"条件运算":"没有该值";